# Supplier

`Supplier` bao gồm danh sách các nhà cung cấp dành cho việc xử lí đơn hàng.

Thông tin nhập của 1 `supplier` chỉ bao gồm

- `Name`: tên supplier.
- `Code`: mã code của supplier (chú ý unique) để phân biệt khi nhập đơn.
